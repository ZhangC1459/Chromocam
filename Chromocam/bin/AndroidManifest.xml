<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.chromocam"
    android:versionCode="0"
    android:versionName="0.1" android:installLocation="auto">

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="21" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".Events"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Archives"
            android:label="@string/app_name"
            android:parentActivityName="com.chromocam.Events">
            <!--  Parent Activity meta-data for API level 4+ -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
           		android:value="com.chromocam.Events"></meta-data>
        </activity>
        <activity
            android:name=".Live"
            android:label="@string/app_name"
            android:parentActivityName="com.chromocam.Events">
            <!--  Parent Activity meta-data for API level 4+ -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
           		android:value="com.chromocam.Events"></meta-data>
        </activity>
    </application>

</manifest>
